export interface typeUser {
    id: number;
    name: string;
    email: string;
    passwordhash: string;
}
export interface typeBook {
    id: number;
    name: string;
    descricao: string;
    preco: string;
    autor: string;
}